module.exports = async function (client, channel, user) {
    //console.log(`${user.username} reacted with "${reaction.emoji.name}".`); 
console.log(`${user.username} пытается написать в ${channel.name}`)
}